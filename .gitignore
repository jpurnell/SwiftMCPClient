.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/
.env
.netrc
development-guidelines/
development-guidelines/07_LIBRARY/latestReport.json

*.backup-*

# Claude Code local settings (personal preferences, not shared)
.claude/settings.local.json
CLAUDE.local.md
/.claude/skills/
/development-guidelines.pre-v2/

# Local field notes — never committed (career/external context lives here)
project/notes/

# Working notes and local agent configuration. Kept on disk, out of the repository: the
# planning docs are notes about a library rather than part of it, and the Claude settings are
# one machine's permission rules. Both were stripped from history when this repository became
# public, so nothing here re-adds them by accident.
project/
.claude/
